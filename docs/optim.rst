.. _sepiaoptim:

SepiaOptim
==========

The `SepiaOptim` class implements two gradient-free optimizers for optimizing the sepia model log posterior with
respect to the parameters (Nelder-Mead simplex and particle swarm optimization).

.. autoclass:: sepia.SepiaOptim
    :members: