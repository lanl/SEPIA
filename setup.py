from setuptools import setup

setup(name='sepia', version='0.1', description='Gaussian process emulation and calibration',
      url='', author='', author_email='', license='', packages=['sepia'], zip_safe=False)

# Usage locally while in dev:
#     pip install -e <path to sepia>/sepia
